services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5612:5000"
    environment:
      - SALT_KEY=${SALT_KEY}
    volumes:
      - ./data:/data
    restart: unless-stopped
